var global = require('global');
var app = require('appModule');
var url = require('util/url.js');

var compareService = app.service('compareService', function ($http) {

});

module.exports = compareService;
